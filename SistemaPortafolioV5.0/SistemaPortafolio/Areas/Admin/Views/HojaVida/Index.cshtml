@model IEnumerable<SistemaPortafolio.Models.HojaVida>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Persona.dni)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Persona.dni)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.hojavida_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.hojavida_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.hojavida_id })
        </td>
    </tr>
}

</table>
